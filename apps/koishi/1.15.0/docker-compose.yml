services:
  koishi:
    image: koishijs/koishi:v1.15.0
    container_name: ${CONTAINER_NAME}
    restart: always
    environment:
      - TZ=${TIME_ZONE}
    networks:
      - corapanel-network
    ports:
      - ${PANEL_APP_PORT_HTTP}:5140
    volumes:
      - ./data:/koishi
    labels:  
      createdBy: "Apps"
networks:
    corapanel-network:
        external: true
